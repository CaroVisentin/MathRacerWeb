// Tipos de wildcards disponibles en el juego
export enum WildcardType {
    RemoveWrongOption = 1, // Elimina una opción incorrecta (ID 1)
    SkipQuestion = 2, // Cambia la ecuación actual sin penalización (ID 2)
    DoubleProgress = 3 // La siguiente respuesta correcta vale doble (ID 3)
}
